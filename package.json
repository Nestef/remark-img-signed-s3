{
  "name": "remark-img-signed-s3",
  "version": "0.1.0",
  "description": "remark plugin to parse image object keys to signed aws s3 urls",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "unist-util-visit": "3.0.1"
  }
}
